allprojects {
    repositories {
        jcenter()
        maven {
            url 'https://github.com/MegaMek/mavenrepo/raw/master'
        }
    }
}

subprojects {
    group 'org.megamek'
    version '1.0-SNAPSHOT'
}